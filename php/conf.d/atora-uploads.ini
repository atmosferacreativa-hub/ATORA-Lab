; Local dev: increase upload limits so WordPress plugin ZIPs > 2MB can be uploaded.
upload_max_filesize = 64M
post_max_size = 64M

; Optional, but helps with slower machines / bigger installs.
memory_limit = 512M
max_execution_time = 600
max_input_time = 600
